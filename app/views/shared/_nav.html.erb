<header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <%= link_to "SeraCyFiniSki", "#", id: "logo" %>
        <nav>
          <ul class="nav  navbar-nav navbar-right">
            <li><%= link_to "Home",   "#" %></li>
            <li><%= link_to "Help",   "#" %></li>
            <li><%= link_to "About",  "#" %></li>
            <% if signed_in? %>
              <li class="dropdown">
                <a href ="#" class="dropdown-toggle" data-toggle="dropdown">Account<b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%=link_to "Profile", user_path(id: current_user.id)%></li>
                  <li><%=link_to "Settings",  user_edit_path(current_user)%></li>
                  <li class='divider'></li>
                    <li> <%=link_to "Log out", "#"%></li>
                  </li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to "Log in", sign_in_path %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>